interface:
  display_name: "rsctf Challenge Playtest"
  short_description: "Prepare and run isolated blind playtests"
  default_prompt: "Use $rsctf-challenge-playtest to prepare and run a player-equivalent blind playtest."
policy:
  allow_implicit_invocation: true
