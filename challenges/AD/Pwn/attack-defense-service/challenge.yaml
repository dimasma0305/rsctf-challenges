# Platform-hosted AttackDefense example. rsctf launches one service per accepted
# team, runs checker/run.py each round, and exposes team targets through A&D.
name: "Attack & Defense Demo: Raw TCP Flag Service"
author: "rsctf documentation team"
description: |
  Defend your service and retrieve rotating flags from the other teams.

  rsctf writes the current round flag to RSCTF_FLAG_FILE. The service accepts one
  newline-framed command per TCP connection: PING returns PONG, and GET_FLAG
  returns the current flag. GET_FLAG is intentionally unauthenticated: this is
  an educational smoke test for A&D orchestration, not a production challenge.
type: AttackDefense
category: Pwn

# A&D score comes from attacks, defense, and SLA rather than jeopardy decay.
disableBloodBonus: true

# A&D flags use rsctf's fixed per-round `flag{<32 URL-safe characters>}`
# grammar. Unlike DynamicContainer, this mode does not accept a flag template.

container:
  # containerImage is intentionally omitted. Repository Bindings auto-builds
  # ./src/Dockerfile when this challenge is imported.
  memoryLimit: 64
  cpuCount: 1
  exposePort: 8080
  enableTrafficCapture: false

ad:
  # The importer prepares checker/{run.py,lib.py,requirements.txt}, installs the
  # exact dependency pin, and stores that environment on the challenge.
  allowEgress: true
  allowSelfReset: true
  # false means rsctf hosts the per-team service. true selects the BYOC workflow.
  selfHosted: false
